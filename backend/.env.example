# DermAI Environment Configuration Template
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# ============================================================================
# CRITICAL SECRETS - CHANGE IN PRODUCTION
# ============================================================================

# Database Configuration
# Generate a strong password for production
DATABASE_URL=postgresql://dermai_user:your_secure_db_password_here@localhost:5432/dermai_db

# Redis Configuration
# Generate a strong password for production
REDIS_URL=redis://:your_secure_redis_password_here@localhost:6379/0

# JWT Secret Key
# Generate with: openssl rand -hex 32
SECRET_KEY=your_secure_secret_key_change_in_production_use_openssl_rand_hex_32

# ============================================================================
# CELERY CONFIGURATION
# ============================================================================

# Celery Broker (should match REDIS_URL)
CELERY_BROKER_URL=redis://:your_secure_redis_password_here@localhost:6379/0

# Celery Result Backend (should match REDIS_URL)
CELERY_RESULT_BACKEND=redis://:your_secure_redis_password_here@localhost:6379/0

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================

# Application environment (development, staging, production)
ENVIRONMENT=development

# API settings
API_V1_STR=/api/v1
PROJECT_NAME=DermAI
PROJECT_VERSION=0.1.0

# ============================================================================
# EXTERNAL API KEYS (Optional, but recommended for production)
# ============================================================================

# Anthropic API Key (for Claude AI analysis)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API Key (fallback for GPT-4o)
OPENAI_API_KEY=your_openai_api_key_here

# ============================================================================
# MONITORING & LOGGING
# ============================================================================

# Sentry DSN (error tracking and monitoring)
SENTRY_DSN=

# Logging level
LOG_LEVEL=INFO

# ============================================================================
# DATABASE SETTINGS
# ============================================================================

# PostgreSQL connection pool settings
DB_ECHO=false
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10

# ============================================================================
# CORS & SECURITY
# ============================================================================

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# ============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ============================================================================

# When deploying to production, ensure:
# 1. Generate a new SECRET_KEY with: openssl rand -hex 32
# 2. Use strong, randomly-generated database password
# 3. Use strong, randomly-generated Redis password
# 4. Set ENVIRONMENT=production
# 5. Configure proper database backups
# 6. Set up SSL/TLS certificates
# 7. Use secrets management (AWS Secrets Manager, HashiCorp Vault, etc.)
# 8. Enable monitoring and alerting
# 9. Configure log aggregation
# 10. Never commit .env to version control
